let letra = prompt('Digite uma letra');
letra = letra.toLowerCase().trim();

    if (letra == 'a' || letra == 'e' || letra == 'i' || letra == 'o' || letra == 'u') { alert('A letra digitada é uma vogal'); }
    else { alert('A letra digitada é uma consoante'); }

/*
let letra = promt('Informe uma letra');

letra = letra.toLowerCase().trim();

let resposta = 'A letra é uma consoante';

if (letra == 'a' ||
    letra == 'e' ||
    letra == 'i' ||
    letra == 'o' ||
    letra == 'u') {
        resposa = 'A letra é uma vogal'
    }

alert(resposta);
*/